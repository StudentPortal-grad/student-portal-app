import 'package:flutter/material.dart';

class SearchedPosts extends StatelessWidget {
  const SearchedPosts({super.key});

  @override
  Widget build(BuildContext context) {
    return SingleChildScrollView(
      child: Column(
        children: [],
      ),
    );
  }
}
